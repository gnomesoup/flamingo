<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Flamingo: Manage Transforms" 
        Height="500" 
        Width="650"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        Background="#fc9090">

    <Window.Resources>
        <!-- Define shared styles -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#cccccc"/>
            <Setter Property="Foreground" Value="#0c1d41"/>
            <Setter Property="BorderBrush" Value="#0c1d41"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="3"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#0c1d41"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0c1d41"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#ecb02f"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#fe4015"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#cccccc"/>
                                <Setter Property="Foreground" Value="#777777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Background" Value="#fe4015"/>
            <Setter Property="BorderBrush" Value="#0c1d41"/>
            <Setter Property="BorderThickness" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#ecb02f"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#fe4015"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#cccccc"/>
                                <Setter Property="Foreground" Value="#777777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#cccccc"/>
            <Setter Property="Foreground" Value="#0c1d41"/>
            <Setter Property="BorderBrush" Value="#0c1d41"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="3"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="#cccccc"/>
            <Setter Property="Foreground" Value="#0c1d41"/>
            <Setter Property="BorderBrush" Value="#0c1d41"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            Margin="2,0"/>
                        </Border>
                        <ControlTemplate.Triggers>

                            <!-- Selected (when ListBox does not have keyboard focus) -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="#ECB02F"/>
                                <Setter Property="Foreground" Value="#0c1d41"/>
                            </Trigger>

                            <!-- Selected + active/focused selection (keyboard focus on the ListBox) -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="Selector.IsSelectionActive" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#ECB02F"/> <!-- focused selected color -->
                                <Setter Property="Foreground" Value="#0c1d41"/>
                            </MultiTrigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
        </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Left Panel - Preset List -->
        <StackPanel Grid.Column="0" Grid.Row="0" Grid.RowSpan="8" Margin="0,0,10,0">
            <Label Content="Transform Presets:" FontWeight="Bold"/>
            <ListBox x:Name="preset_listbox" 
                     Height="300" 
                     SelectionChanged="on_preset_selected"/>
            
            <StackPanel Margin="0,10,0,0">
                <Button x:Name="btn_add" 
                        Content="Add New" 
                        Margin="0,0,0,5" 
                        Click="on_add_preset"/>
                <Button x:Name="btn_remove" 
                        Content="Remove" 
                        Click="on_remove_preset"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Right Panel - Edit Fields -->
        <Label Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="2"
               Margin="0,25,0,0"
               Content="Preset Name:"/>
        <TextBox x:Name="txt_name" 
                 Grid.Column="1" 
                 Grid.Row="0"
                 Grid.ColumnSpan="2"
                 Margin="0,50,0,0"
                 Height="30"
                 IsEnabled="False"/>
        
        <Label Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2"
               Content="Origin (X, Y, Z):"/>
        <TextBox x:Name="txt_origin" 
                 Grid.Column="1" 
                 Grid.Row="1" 
                 Margin="0,25,0,0"
                 Height="30"
                 IsEnabled="False"/>
        <Button x:Name="btn_pick_origin"
                Grid.Column="2"
                Grid.Row="1"
                Content="Pick"
                Margin="0,25,0,0"
                Width="50"
                Height="30"
                Click="on_pick_origin"
                IsEnabled="False"/>
        
        <Label Grid.Column="1" Grid.Row="2" Grid.ColumnSpan="2"
               Content="Target (X, Y, Z):"/>
        <TextBox x:Name="txt_target" 
                 Grid.Column="1" 
                 Grid.Row="2" 
                 Margin="0,25,0,0"
                 Height="30"
                 IsEnabled="False"/>
        <Button x:Name="btn_pick_target"
                Grid.Column="2"
                Grid.Row="2"
                Content="Pick"
                Width="50"
                Height="30"
                Margin="0,25,0,0"
                Click="on_pick_target"
                IsEnabled="False"/>
        
        <Label Grid.Column="1" Grid.Row="3" Grid.ColumnSpan="2"
               Content="Angle (degrees):"/>
        <TextBox x:Name="txt_angle" 
                 Grid.Column="1" 
                 Grid.Row="3"
                 Grid.ColumnSpan="2"
                 VerticalAlignment="Center"
                 Margin="0,25,0,0"
                 Height="30"
                 IsEnabled="False"/>
        
        <Label Grid.Column="1" Grid.Row="4" Grid.ColumnSpan="2"
               Content="Scale:"/>
        <TextBox x:Name="txt_scale" 
                 Grid.Column="1" 
                 Grid.Row="4"
                 Grid.ColumnSpan="2"
                 Margin="0,25,0,0"
                 Height="30"
                 IsEnabled="False"/>
        
        <Button x:Name="btn_map_points" 
                Grid.Column="1" 
                Grid.Row="5"
                Grid.ColumnSpan="2"
                Content="Map Points" 
                Margin="0,10,0,0"
                Height="30"
                Click="on_map_points"
                IsEnabled="False"/>
        
        <Button x:Name="btn_save" 
                Grid.Column="1" 
                Grid.Row="6"
                Grid.ColumnSpan="2"
                Content="Save Preset" 
                Margin="0,5,0,0"
                Height="30"
                Click="on_save_preset"
                IsEnabled="False"/>
        
        <Button x:Name="btn_save_all" 
                Style="{StaticResource PrimaryButtonStyle}" 
                Grid.Column="1" 
                Grid.Row="7"
                Grid.ColumnSpan="2"
                VerticalAlignment="Bottom"
                Content="Done" 
                Margin="0,5,0,0"
                Height="50"
                Click="on_save_all"/>
    </Grid>
</Window>